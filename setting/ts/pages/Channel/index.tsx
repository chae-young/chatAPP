import Workspace from '@layouts/Workspace';
import React from 'react';

const Channel = () => {
    return (
        <Workspace>
            <div>채널</div>
        </Workspace>
    );
};

export default Channel;